steps:
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  args:
  - gcloud
  - functions
  - deploy
  - hello_world
  - --region=europe-west1
  - --source=.
  - --trigger-http
  - --runtime=python310
  - --trigger-event=google.storage.object.finalize